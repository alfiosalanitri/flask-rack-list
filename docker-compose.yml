services:
  app:
    build: .
    container_name: racklist
    ports:
      - "7002:5000"
    env_file:
      - .env
    volumes:
      # SQLite database persisted across container restarts
      - racklist-data:/data
    restart: always

volumes:
  racklist-data:
